🚀 突破極限的 SOTA 方法一：HTC (Hybrid Task Cascade) 升級
現象診斷：
Mask R-CNN 的瓶頸在於它的兩顆預測頭（邊界框頭、遮罩頭）是平行的。一旦 Bounding Box 框得不準，Mask 預測一定會爛掉。這在細胞重疊的區域特別嚴重。

解決方案：
如果你能使用 MMDetection 框架，將架構從單純的 Mask R-CNN 升級為 Hybrid Task Cascade (HTC)，分數會迎來巨大突破。

串聯精煉 (Cascade)：它串聯了 3 個 IoU 閾值遞增的預測頭（0.5 -> 0.6 -> 0.7），強迫模型把細胞邊界框逼近完美。

遮罩資訊流動 (Task Intertwining)：HTC 會把前一個階段預測出來的 Mask 特徵，傳遞給下一個階段。這解決了 Mask R-CNN 中「框準了，但 Mask 畫太醜」的缺陷。

（如果你必須堅持純 PyTorch/Torchvision 實作，要手刻 HTC 的難度極高。但如果你可以引入 MMDetection，這將是首選。）

突破極限的 SOTA 方法二：自適應偽標籤與半監督學習 (Self-Training / Pseudo-Labeling)
現象診斷：
你只有 200 張 Training Data。就算你用了 Copy-Paste（複製貼上增強），模型看到的「細胞多樣性」依舊受限於這 200 張圖的原始分佈。

SOTA 解決方案 (冠軍級策略)：
第一名能拿到 0.53，極有可能利用了 Test Set（通常在比賽中有數千張未標註的圖片）。

Teacher Model：用你現在訓練出來的最強模型（AP50 > 0.35 的那一顆），對所有 test_release 的圖片進行預測。

置信度過濾 (Confidence Filtering)：將測試集中預測分數極高（例如 score > 0.9）且形狀完整的 Mask 篩選出來。

重新訓練 (Student Model)：將這些篩選出來的「偽標籤 (Pseudo-Labels)」加入你原本的 200 張 Train Dataset 中。用一個全新的模型（或重置權重）去訓練這組更龐大的資料集。

這個方法被稱為 Noisy Student Training，它是克服資料匱乏最直接有效的魔法，能強迫模型吸收 Test Set 的真實分佈，解決領域偏移 (Domain Gap) 的問題。